[project]
name = "odometer-experiments"
version = "0.1.0"
description = "Mechanistic-interpretability experiments on Llama-3.2 counting failures"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "transformers>=4.45",
    "accelerate>=0.30",
    "huggingface-hub>=0.24",
    "scikit-learn>=1.3",
    "matplotlib>=3.8",
    "numpy>=1.26",
]

[tool.uv]
package = false
